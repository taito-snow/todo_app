要件
・新規タスクの入力
・終了チェックボックス （テキスト斜線）
・重要度の高いものにスターをつける
・削除機能

docker
.continar todo_php todo_mysql
.network todo-network

・mysql

create table todos (
    id int not null auto_increment primary key,
    state tinyint(1) default 0, /* 0:not finished, 1:finished */
    title text,
    star  tinyint(1) default 0
);